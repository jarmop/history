import React from 'react';
import styles from './App.module.css';

const App: React.FC = () => {
    return (
        <div className={styles.app}>
            History app
        </div>
    );
};

export default App;
